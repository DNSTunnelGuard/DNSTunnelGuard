[project]
name = "control-plane"
version = "0.1.0"
description = "Control plane for DNS tunnel guard"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dnslib>=0.9.26",
    "flask>=3.1.3",
    "pytest>=9.0.2",
    "requests>=2.32.5",
]
