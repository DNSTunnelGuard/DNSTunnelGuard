# Connecting Servers
flask

# Core ML / NLP
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=1.1.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Optional: TensorBoard logging (used via TENSORBOARD_LOGGING_DIR env var)
tensorboard>=2.14.0